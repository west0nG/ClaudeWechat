# Quote Reply (引用回复)

```
📸 1. screenshot → find target message
   2. right-click on message bubble (near center, not edge)
📸 3. screenshot → click "Quote" / "引用" (NOT "Forward...")
   4. computer_batch: [type reply, Return]
```

Cursor auto-focuses in input after clicking Quote.
